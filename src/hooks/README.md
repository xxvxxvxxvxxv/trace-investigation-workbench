# UI state

`inspectorHistory.ts` contains the pure, bounded inspector navigation reducer and workspace/case context classification. The desktop inspector and narrow drawer share the same record content and history. Local sidebar collapse is a presentation preference, separate from the case database.
